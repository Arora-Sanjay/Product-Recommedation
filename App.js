import { Text, SafeAreaView, StyleSheet, View, TextInput, ActivityIndicator, ScrollView, Button } from 'react-native';
import AdvisorScreen from './src/AdvisorScreen'

export default function App() {

  return (
    <SafeAreaView>
      <AdvisorScreen />
    </SafeAreaView>
  );
}

